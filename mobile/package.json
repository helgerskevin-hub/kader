{
  "name": "crypto-copy-trading-mobile",
  "version": "1.0.0",
  "description": "Crypto Copy-Trading analyse-app voor Android (Capacitor)",
  "private": true,
  "scripts": {
    "sync": "cap sync android",
    "build:apk": "cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/core": "^6.2.1",
    "@capacitor/local-notifications": "^6.1.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.1"
  }
}
